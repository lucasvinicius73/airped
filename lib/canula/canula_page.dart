import 'package:flutter/material.dart';

class CanulaPage extends StatelessWidget {
  const CanulaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Canula de Guedel"),
        actions: [],
      ),
    );
  }
}